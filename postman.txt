Postman is an API platform for building and using APIs.
Postman simplifies each step of the API lifecycle and streamlines collaboration so you can create better APIs—faster.
Tool for creating backend routes
tool for testing backend routes

In Cases when we don't have frontend ready but we want to build up the backend, postman will help us to visualize results/

example 1 steps:

start the nodemon server: nodemon methods.js
go to postman and send the POST request, it will put name: ajaydeep in user object
then to see it make a GET request in postman
Now make a PATCH by putting some value in body, then send a patch request, which will add age:1000 in user object
Make GET request to see it.